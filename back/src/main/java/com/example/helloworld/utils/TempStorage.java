package com.example.helloworld.utils;

import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpSession;
import org.springframework.stereotype.Service;

public class TempStorage {

    /*public static final String AUTH_CODE_KEY = "authCode";

    public void saveAuthCodeToSession(HttpServletRequest request, String email, String authCode) {
        HttpSession session = request.getSession();
        session.setAttribute(email, authCode);
    }

    public String getAuthCodeFromSession(HttpServletRequest request, String email) {
        HttpSession session = request.getSession();
        return (String) session.getAttribute(email);
    }*/
}
